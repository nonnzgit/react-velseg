import { colors } from "../../globalStyles";
import styled from "styled-components";

export const ScSelectorFecha = styled.div`
  background: ${colors.gradient};
  color: white;
  width: 18rem;
  margin-bottom: 1rem;
  border-radius: 10px;
  display: flex;
  justify-content: center;
  align-items: center;
`;
